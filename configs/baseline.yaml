# Example baseline config — adjust paths and seeds when wiring real code.
project:
  seed: 42
  output_dir: results

data:
  manifest_csv: data/processed/manifest_with_split.csv  # from scripts/02_make_splits.py

evaluation:
  metrics:
    - VSR
    - BA
    - harmful_degree
    - MCD
